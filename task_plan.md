# 任务计划

## 目标

对 `SuperClash.ini` 做专业化整理：基于仓库用途审查配置，删除陈旧或含混逻辑，统一结构与命名，并完成可执行的语法和关键行为验证。

## 阶段

| 阶段 | 状态 | 内容 |
|---|---|---|
| 1. 现状审计 | complete | 阅读配置与文档，识别格式、语义和维护性问题 |
| 2. 配置重构 | in_progress | 按审计结果修改完整相关链路，保持逻辑简单明确 |
| 3. 验证交付 | pending | 运行可用校验，复核差异并记录结果 |

## 决策

- 不保留旧配置与新配置双轨逻辑。
- 外部订阅和远程规则仅在输入边界做必要校验。
- 未经证据支持，不猜测新增业务策略。
- 保留现有中国、媒体和通用代理规则覆盖，避免没有业务依据的路由变化。
- 删除内容为空的 `ban.list` 远程引用。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---|---|
| `web__run` 搜索返回 HTTP 405 | 1 | 改用官方 GitHub 仓库 Raw/API 端点只读核验 |
| 首版重构误给文本 `.list` 添加 `clash-classic:`，并漏掉 `direct.list` | 1 | 静态复核发现，验证前纠正类型并恢复自定义直连规则 |
| 动态转换的本地订阅未被识别 | 3 | `file://`、相对路径均未进入解析；停止本地文件尝试，改用回环 HTTP 输入 |
